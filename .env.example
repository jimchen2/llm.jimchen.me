# .env
# Required
REDIS_URL=redis://localhost:6379
APP_PASSWORD=my_secure_password

# Optional
# LLM_TIMEOUT_MS=120000   # LLM request timeout, defaults to 120s
# CACHE_TTL_SECONDS=86400 # how long conversations live in Redis
# GEMINI_API_ENDPOINT=https://generativelanguage.googleapis.com/v1beta/models
